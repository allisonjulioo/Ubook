import { createStore } from 'vuex';
import { ModalStore } from './modules/modal';

export const store = createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    ModalStore,
  },
});
